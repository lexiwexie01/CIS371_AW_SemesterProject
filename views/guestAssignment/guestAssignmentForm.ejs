<!-- Alexis Webster -->
<% 
  if (assignment.aid) {
    submitLabel = "Update Guest Assignment";
    route = `/assignment-scheduler/guest/${assignment.aid}`;
  } else {
    submitLabel = "Create Guest Assignment";
    route = `/assignment-scheduler/guest/new}`;
  }
%>
                    <div class=" field ">
                        <label for="assignment_name ">Name</label>
                        <input type="text" name="assignment[name]" id="name" value="<%= assignment.name %>" required>
                    </div>

                    <div class="padding"></div>

                    <div class="actions">
                        <input type="submit" name="commit" value="<%= submitLabel %>" />
                    </div>
                </form>